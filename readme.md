# vim

usage
-----

```bash
$ cd ~
$ git clone https://github.com/neggiel/tmux .tmux
$ cd .tmux
$ git clone https://github.com/tmux-plugins/tpm ./plugins/tpm
$ cd ..
$ ln -s /path/to/home/.tmux/.tmux.conf .
$ tmux
prefix I(shift+I)
```
